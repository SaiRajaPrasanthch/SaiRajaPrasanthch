import 'package:flutter/material.dart';

class  ChatFileDisplay extends StatelessWidget {
  // final List<attachment>
  const ChatFileDisplay({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}